#!/usr/bin/env python

import os
import sys

bloghome = os.getenv('BLOG_HOME', '.')
os.chdir(bloghome)
os.execlp('jekyll', 'jekyll', '--pygments', '--no-lsi', '--safe', '--serve',
          '--auto', *sys.argv[1:])
