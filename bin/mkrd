#!/bin/sh

if [ $# != 2 ]; then
    echo "$0 ramdisk uImage"
    exit
fi

rm -f /tmp/ramdisk.img
rm -f /tmp/boot.img
 
mkimage -A arm -O linux -T ramdisk -C none -d $1 /tmp/ramdisk.img
mkimage -A arm -O linux -C none -T multi -d $2:/tmp/ramdisk.img /tmp/boot.img
